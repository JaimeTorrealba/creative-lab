import { generateRoute } from '../utils'

const descriptions = {
  template: 'Initial template for WebGpu demos',
}

export const webgpu_demos = [
  generateRoute('template', 'webGpu', descriptions.template),
]
